<Window x:Class="WpfDemo.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="MainView"
        Width="800"
        Height="450"
        mc:Ignorable="d">
    <Grid>
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Orientation="Vertical">
            <Button Click="AnimationButton_Click"
                    Content="Animation" />
            <Button Click="CornerRadiusButton_Click"
                    Content="CornerRadius" />
            <Button Click="ShadowButton_Click"
                    Content="Shadow" />
            <Button Click="ProgressButton_Click"
                    Content="Progress" />
            <Button Click="LazyLoadingButton_Click"
                    Content="Lazy Loading" />
            <Button Click="MemoryUsageButton_Click"
                    Content="Memory Usage" />
            <Button Click="StretchButton_Click"
                    Content="Stretch" />
        </StackPanel>
    </Grid>
</Window>